# Deliberately minimal. The competing-risks estimators in src/incidence.py and the
# backfitting in src/lender.py are implemented directly rather than pulled from a
# library, so the difference between 1-KM and Aalen-Johansen is inspectable in the
# source instead of hidden behind a call.
pandas==2.2.3
numpy==2.0.2
pyarrow==21.0.0
matplotlib==3.9.4
pytest==8.4.2
